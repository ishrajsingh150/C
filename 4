#include <stdio.h>
#include <string.h>

int main() {
    char a[50],b[50];
    gets(a);
    gets(b);

    printf("Copy: %s\n", strcpy(a,b));
    printf("Concat: %s\n", strcat(a,b));
    printf("Compare: %d\n", strcmp(a,b));

    return 0;
}
